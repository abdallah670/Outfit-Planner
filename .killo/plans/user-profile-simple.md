[{'Missing': 'GET /api/user/users/{id'}, {'Returns': 'PublicUserProfileDto\n```\n\n### DTOs to Create\n\n**PublicUserProfileDto.cs**\n```csharp\npublic class PublicUserProfileDto\n{\n    public string Id { get; set;'}, {'Note': 'StylePreference` enum already exists in `OutfitPlanner.Domain.Enums`.\n\n---\n\n### Query & Handler\n\n**GetPublicUserProfileQuery.cs**\n```csharp\npublic class GetPublicUserProfileQuery : IRequest<PublicUserProfileDto?>\n{\n    public string UserId { get; set;'}, {'Note': "If `IUserRepository` doesn't include navigation properties (WardrobeItems", 'queries': 'csharp\nvar wardrobeItemCount = await _userRepository.CountWardrobeItemsAsync(userId);\nvar outfitCount = await _userRepository.CountOutfitsAsync(userId);\nvar totalWears = await _userRepository.GetTotalWearsAsync(userId);\n```\n\n---\n\n### Controller Endpoint\n\nAdd to **UserController.cs**:\n```csharp\n/// <summary>\n/// Get public profile information for any user\n/// </summary>\n[HttpGet(', 'users/{userId}/profile': ['AllowAnonymous]\npublic async Task<ActionResult<PublicUserProfileDto?>> GetPublicProfile(string userId)\n{\n    var query = new GetPublicUserProfileQuery { UserId = userId };\n    var profile = await _mediator.Send(query);\n    \n    if (profile == null)\n        return NotFound(new { message = "User not found']}, {'Performance': "Ensure indexes on `Follow` table:\n- `IX_Follow_FollowingId` on `FollowingId`\n- `IX_Follow_FollowerId` on `FollowerId`\n\n### 2. User Repository Count Methods (if needed)\n\nIf user repository doesn't expose counts:\n```csharp\nTask<int> CountWardrobeItemsAsync(string userId);\nTask<int> CountOutfitsAsync(string userId);\nTask<int> GetTotalWearsAsync(string userId);\n```\n\n---\n\n## Frontend Integration (summary)\n\n1. **SocialDataSource** add:\n   ```ts\n   getPublicProfile(userId: string): Observable<PublicUserProfileDto>\n   ```\n\n2. **SocialRepository** / **SocialUseCases** add same method.\n\n3. **ProfileComponent**:\n   - If route has `userId` param:\n     - Call `socialRepository.getPublicProfile(userId)` to get stats\n     - Display: name", 'TrendingOutfitsComponent**': 'Navigate to profile with `userId` param and route state for quick display\n\n---\n\n## Testing Checklist\n\n- [ ] `GET /api/user/users/{userId'}, ["Returns 404 when userId doesn't exist\n- [ ] Followers/Following counts are accurate\n- [ ] Sensitive fields (Email, Preferences) are NOT in response\n- [ ] CORS allows access from frontend\n- [ ] Performance: profile load < 500ms for average user\n- [ ] Indexes exist on Follow table for count queries\n\n---\n\n## Implementation Order (2-3 hours)\n\n1. Create `PublicUserProfileDto.cs`\n2. Add `IFollowRepository` count methods and implementation\n3. (Optional) Add user repository count methods if needed\n4. Create `GetPublicUserProfileQuery` & Handler\n5. Add `UserController.GetPublicProfile` endpoint\n6. Run integration tests\n7. Frontend: Add `getPublicProfile` to Social layer\n8. Frontend: Update ProfileComponent to fetch and display public profile\n\n---\n\n## Notes\n\n- Follow counts are **readily available** via existing `GetFollowers`/`GetFollowing` endpoints' `totalCount` field, but those return paginated lists. We need just counts. Counting rows is cheap with proper indexes.\n- For MVP, embed counts in `PublicUserProfileDto`. If counts become heavy later, denormalize to User table.\n- The design shows follower/following counts prominently on profile—these must be included.\n\n---\n\n**End of Plan**"]]