# Plan: Redesign public-profile page to match social design/userprofile.html

## Overview
Redesign `public-profile.component.html` and `public-profile.component.scss` to match the social design spec in `social desgin/userprofile.html`. The design follows a modern social-media style with a clean white card layout, grid-based outfit gallery, pill-style stats, and tab navigation.

## Key Changes

### 1. HTML Template (`public-profile.component.html`)

**Global structure:**
- Wrap entire page in a flex column container with `bg-[#fafafb]` background
- Full-width max-w-[1200px] centered layout with `px-6 pb-20`

**Top navigation bar (new):**
- Add a navbar row: brand logo (Outfit Planner), nav links (My Wardrobe, Plan Outfits, Calendar, Social, Settings), search icon, notification icon with badge, user avatar circle
- Use `flex items-center justify-between` with `h-20 bg-background px-8`

**Back button:**
- Replace current circular back button with a rectangular button: `flex h-10 w-10 items-center justify-center rounded-lg bg-white border border-border text-foreground hover:bg-muted shadow-sm`
- Contains a `lucide:arrow-left` iconify icon

**Profile card:**
- Change outer card to: `rounded-xl bg-white p-8 border border-border shadow-sm`
- Layout: `flex flex-col lg:flex-row items-center lg:items-center justify-between gap-8`
- Avatar section: `relative h-28 w-28 shrink-0 overflow-hidden rounded-full border-4 border-[#e6f0f9]` with 112px image
- Info section: `flex flex-col items-center sm:items-start pt-1`
  - Display name: `text-[28px] font-bold font-headings text-foreground leading-tight`
  - Handle: `text-[15px] text-muted-foreground mt-1 mb-3`
  - Bio: `text-[15px] text-foreground mb-5 text-center sm:text-left max-w-[400px] leading-relaxed`
  - Follow button: `flex h-[38px] items-center justify-center gap-2 rounded-md bg-primary px-6 text-[14px] font-semibold text-white shadow-sm hover:opacity-90 transition-opacity` with `lucide:user-plus` icon

**Stats row:**
- Horizontal flex row with `items-center gap-6 sm:gap-10 mt-6 lg:mt-0 px-4 self-center lg:self-start pt-6`
- Each stat: flex column items-center with `text-[28px] font-black text-foreground leading-none` for value and `text-[13px] text-muted-foreground mt-2` for label
- Vertical dividers: `h-10 w-px bg-border` between stats
- Labels: "Outfits", "Items", "Followers", "Following"

**Tabs navigation:**
- Flex row: `flex w-full items-center gap-8 border-b border-border text-sm font-bold text-muted-foreground mt-8 mb-6 overflow-x-auto`
- Each tab button: `flex h-12 items-center justify-center whitespace-nowrap transition-colors border-b-2 border-transparent hover:text-foreground`
- Active tab: `border-b-2 border-primary text-primary`
- Tab labels: "Outfits (count)", "Activity", "Followers", "Following"

**Activity tab - Outfit Grid (replaces current post-list):**
- Grid layout: `grid w-full grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 pb-20`
- Each outfit card: `group flex flex-col overflow-hidden rounded-xl bg-white border border-border shadow-sm transition-transform hover:-translate-y-1 hover:shadow-md cursor-pointer`
  - Image area: `relative aspect-[3/4] w-full overflow-hidden bg-muted` with image
  - Category badge: `absolute top-4 right-4 rounded-full bg-white/90 px-3 py-1 text-xs font-bold text-foreground shadow-sm backdrop-blur-sm`
  - Info below: `flex flex-col p-4 bg-white`
    - Title: `text-[15px] font-bold text-foreground mb-1`
    - Meta row: `flex items-center justify-between mt-1` with season/items text (`text-[13px] text-muted-foreground`) and like count with `lucide:heart` icon
- Add outfit count badges to tab titles (Outfits (38))

**Followers/Following tab content:**
- Keep user list layout but update styling to match new design language: `rounded-xl bg-white border border-border shadow-sm`
- Each user item: `flex items-center gap-4 p-4` with rounded avatar `h-12 w-12`

**Empty states:**
- Update to match new design with appropriate icon sizes and spacing

### 2. SCSS Styles (`public-profile.component.scss`)

- Replace most existing styles to match the new Tailwind-equivalent classes
- Add styles for:
  - Navbar (if not using a shared component)
  - New profile card layout
  - Updated avatar sizing (112px)
  - Stats row with dividers
  - Tab navigation with bottom-border active indicator
  - Outfit grid cards with hover effects, aspect-ratio containers, category badges
  - Updated follow button (`h-[38px]`, `rounded-md`)
  - User list items for followers/following tabs

### 3. Component TypeScript (`public-profile.component.ts`)

- No TypeScript logic changes needed (all changes are structural/styling)
- Tab labels may include outfit count

## Files to Modify
1. `src/outfit-planner-ui/src/app/presentation/pages/public-profile/public-profile.component.html`
2. `src/outfit-planner-ui/src/app/presentation/pages/public-profile/public-profile.component.scss`

## Notes
- The target design uses iconify icons (`lucide:*`) instead of Material icons for nav and action icons
- Color scheme uses CSS custom properties (`--color-primary: #fab4c6`, `--color-background: #fafafb`, etc.) — keep consistent with existing `#fab4c6` primary
- Font family is Nunito (already used in current codebase)
- Grid layout: 1 column on small screens, 2 on medium (sm:), 3 on large (lg:)